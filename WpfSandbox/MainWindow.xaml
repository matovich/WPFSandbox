<Window x:Class="WpfSandbox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:common="clr-namespace:WpfSandbox.Common"
        xmlns:local="clr-namespace:WpfSandbox"
        xmlns:views="clr-namespace:WpfSandbox.Views"
        mc:Ignorable="d"
        Title="The WPF Sandbox App" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary>

        </ResourceDictionary>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_View">
                <MenuItem Header="_Data Input" Command="{Binding DisplayMessageCommand}"/>
            </MenuItem>
        </Menu>
        <views:DataInput Grid.Row="1"></views:DataInput>

        
    </Grid>
</Window>
